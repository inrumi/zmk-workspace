name: Build ZMK firmware (nix)
on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: urob/zmk-actions/.github/workflows/build-user-config.yml@main
    with:
      # Set to 'gnuarmemb' (fastest), 'zephyr' or, 'zephyr-full' (most
      # compatible). ZMK Studio requires 'zephyr-full'.
      toolchain: zephyr
